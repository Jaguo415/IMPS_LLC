# Models Folder

This folder is dedicated to storing AI-generated Model profiles and images.

## Purpose

The models folder contains:
- Model JSON files with profile information
- Image files for model representations
- AI-generated model data and assets

## Structure

Store your model data files here to keep the repository organized and maintain a clear separation between model assets and other project files.
